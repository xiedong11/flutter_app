import 'package:flutter/material.dart';

/**
 * 可定制label显示的位置，颜色，尺寸
 */
class CommonLabelView extends CustomPainter {
  @override
  void paint(Canvas canvas, Size size) {}

  @override
  bool shouldRepaint(CustomPainter oldDelegate) {
    return false;
  }
}
